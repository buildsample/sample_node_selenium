language: node_js

node_js:
  - "0.10"
 # - "0.11"

addons:
   firefox: "23.0"

services:
  - selenium

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - firefox --version

#after_script:
 # - "sh -e /etc/init.d/xvfb stop"
  

branches:
 only:
   - master


archive: true

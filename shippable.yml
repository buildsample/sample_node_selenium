language: node_js

node_js:
  - "0.10"
  #- "0.11"

addons:
   firefox: "27.0"

services:
  - selenium

before_script:
  - "export DISPLAY=:99.0"
  - "/etc/init.d/xvfb start"

after_script:
  - "/etc/init.d/xvfb stop"

reset_minion: true


notifications:
  email:
    - exampleone@org.com
